"""
Escreva um algoritmo que pergunte ao usuário se deseja inserir mais uma nota, se a resposta do usuário for "N", o programa fará a média aritmética das notas informadas
e mostrará a média aritmética para o usuário.

obs: Use um contador dentro do laço de repetição para contar a quantidade de iterações (loops)
"""
import os
os.system("cls || clear")


opcao = input("Digite uma nota: ")
contador += 1


       